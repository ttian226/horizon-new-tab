# v0.1.5 — Task 便签画布（Task-backed Sticky Notes）

> 周期开始：2026-05-29
> 主题：**把 Notion task 被忽略的"页面正文"放大成贴在新标签上的便签**
> 分支：`feature/v0.1.5`（起点 main `b7bf61e`）

---

## 战略定位（为什么是这个主题）

延续 v0.1.4 的"Horizon Tab = Notion 工作流的浏览器新标签前哨"。

**洞察**：Notion task 的「页面正文」（page body）几乎没人用——你自己、SyncTask iOS 都忽略它。与其再单独做一个鸡肋的 Markdown Notes（重复造轮子、违背主线），不如**把这个已存在却被埋没的正文放大**：

**「一张便签 = 一条 Notion task 的正文。在新标签上编辑便签 = 编辑那条 task 的正文。」**

- 零新数据模型：便签内容就是 task 的 page body，复用 v0.1.4 的 token / hook / schema
- 发挥新标签页独有优势：一块"抬眼就看见"的环境屏幕，便签贴在这上面才有意义（Notion 做不到）
- 两条腿互补：**列表（todo 面板）管结构化任务，便签管"想一直盯着"的少数 task**
- 顺带高分享性：磨砂半透明便签贴在壁纸上，适合截图（小红书线）

---

## 核心模型（已定）

| 维度 | 决定 | 理由 |
|---|---|---|
| **便签内容** | = Notion task 的 page body，**纯文本**（不做 Markdown）| MD↔Notion block 格式不兼容；纯文本够用且砍掉转换器 |
| **读** | `GET /v1/blocks/{pageId}/children` → 抽文本 → textarea | 简单 |
| **写** | textarea 内容按换行切段 → 生成 `paragraph` block → `PATCH /v1/blocks/{pageId}/children` | 无"整体替换正文"接口，只能重写段落 |
| **富内容护栏** | 正文空/纯段落 → 可编辑；含非段落块（图片/列表/表格/toggle）→ **只读 + "在 Notion 编辑"** | 重写会误删富内容，必须保护 |
| **便签来源（选择模型 A）** | 列表里每条 task 一个"钉到桌面"动作 → 变便签；取消钉收回。新建便签 = 新建 task | 列表当大本营，改动最小、心智清晰；保持"便签必有对应 task"不变式 |
| **便签布局（位置/大小/颜色）** | **本地存**（按设备，chrome.storage / localStorage，复用 GlassCard 套路）| Notion 存不了布局；像素坐标跨屏/跨设备不通用 |
| **正文拉取时机** | 懒加载（钉出/打开便签时才拉，不为所有 task 预拉）| 省 Notion rate limit（3 req/s）|
| **显示位置** | **便签只在 work mode 显示**（复用现有可拖拽 widget 画布）| 避免常驻新标签糊屏；work mode 本就是"摆 widget"的场景 |
| **旧 Markdown Notes** | **彻底移除**（不保留本地 MD 编辑器）| 鸡肋且违背主线；便签 = task 正文取而代之 |

---

## 核心任务

### M1 — 便签画布 + 读 ✅ 已完成（commit `150569d`，打磨 `a6a5bc0`）

- [x] **便签组件**：专用 `StickyNote`——多张、可拖动、可缩放、半透明磨砂、内置 5 色配色
- [x] **"钉到桌面"**：work-mode todo 行 hover 出 📌 → 画布生成便签；显示 emoji + 标题 + 正文
- [x] **读正文**：懒加载 page body blocks → 渲染纯文本
- [x] **布局持久化**：pageId + 位置/大小/颜色存 `chrome.storage.local`；关闭=软隐藏（保留布局），重钉恢复
- [x] **数量上限**：`MAX_STICKIES=8`（按显示中的计）。⚠️ 全局"便签层显隐开关"未做（目前逐张关闭）

### M2 — 便签写回（编辑）✅ 主体完成（commit `8a95ce7`）

- [x] **纯文本编辑**：textarea 失焦写回 Notion 正文（删旧段落 + 追加新段落）；失败保留文字 + 提示，不回滚丢稿
- [x] **富内容护栏**：写回前复查是否纯段落，含富内容 → 只读 + "在 Notion 编辑 ↗"
- [x] **到期日 + 状态**（M2 加项）：底部右侧显示到期日 + 可点完成圈（写回 Notion，乐观+回滚）；完成态删除线 + 压暗
- [ ] **新建便签 = 新建 task**：画布上建空便签 → `POST /pages` 建 task —— **未做（M2 剩余）**

### 便签打磨（随 M1/M2 做的，commit `8a95ce7` / `a6a5bc0`）

- [x] 字体与任务面板统一（JetBrains Mono + 苹方，`.font-task`）
- [x] 列表钉状态：📌 高亮且**用便签颜色**（列表↔画布联动）+ 点击 pin/unpin 切换
- [x] 隐藏便签滚动条（`.no-scrollbar`，可滚不显条）
- [x] 拖拽/缩放限制在视口内（不会拖出可见区）

---

## 顺带做的小项（不属便签主线）

- [x] **彻底移除旧 Markdown Notes**（删 `NotesWidget` + dock 项 + `react-markdown`/`remark-gfm` 依赖）—— 为便签清场，build 通过无残留
- [x] **不再预填个人 Notion DB ID**（commit `f456e61`）—— Database ID 默认空 + 格式提示 placeholder
- [ ] **dev config 死数据清理**：正式包目前把 `development` Firebase config 整个打进 bundle（未启用但带着 dev 项目 id）。按 env 拆配置让 Vite 摇树
- [ ] **首次打开壁纸占位图**（v0.1.4 遗留）：cache 全空时初次打开仍黑屏，加打包低分辨率占位图

---

## 不做 / 待定（推后或需决策）

| 项 | 说明 |
|---|---|
| **Notion OAuth 一键授权** | **最大增长杠杆**（手动贴 token + DB ID 门槛高）。工程量大（要 Cloud Function 做 code 交换）。是否抢在便签前/并行做，**待你决策** |
| **多 Notion DB / DB 选择 UI** | 单 DB 先跑通 |
| **便签布局云同步** | 跨设备同步位置很别扭（屏幕尺寸不同），先本地；以后可只同步"哪些 task 是便签 + 颜色" |
| **Markdown 富格式便签** | v0.1.5 纯文本；富格式 ↔ block 双向留后续 |
| **完整 block 类型读渲染** | M1 先纯文本；富 block 的只读渲染（标题/列表/代码）可后做 |

---

## 风险 & 开放问题

1. **Notion rate limit (3 req/s)**：每张便签一次正文 fetch，多便签 + 编辑写回可能撞墙 → 懒加载 + 请求节流。
2. **写回误删富内容**：核心风险，靠"富内容护栏"兜底；要把"只读"的判定做扎实（检测 blocks 是否全为 paragraph/空）。
3. ✅ **便签与 work-mode widget 共存**：已验证，便签只在 work mode、与 todo widget 同屏正常。
4. **跨设备/跨分辨率布局**：本地像素坐标在大屏/小屏错位 → 先按设备本地存，接受不同设备各自布局。
5. **糊屏**：已有数量上限(8) + 半透明克制；全局显隐开关尚未做。
6. ⚠️ **磨砂接缝/高光影子（已知未修，搁置）**：便签 `backdrop-blur` 在鼠标移动重绘时露出竖向接缝（半透明色底把它暴露出来）。试过 `will-change: transform` 无效、已撤。后备方案：降模糊(`xl`→`md`) / 提底色不透明度(15%→~25%) / 干脆去掉 backdrop-blur 用半透明纯色。

---

## 剩余 / 待决策

> 已定并完成：选择模型 A、纯文本、富内容护栏、work-mode-only、移除旧 MD、标题+正文呈现（emoji+标题在顶部、正文在下）、字体统一。

1. **新建便签 = 新建 task**（M2 剩余）：要不要本期做？
2. **磨砂接缝**：搁置中，决定哪天用后备方案修（降模糊/提不透明度/去 blur）。
3. **OAuth 取舍**：v0.1.5 是否纳入 OAuth（或并行）？还是纯做便签、OAuth 留 v0.1.6？
4. **全局便签显隐开关**：是否需要（目前只能逐张关闭）。

---

## 关联

- 上期计划：`v0.1.4-plan.md`
- Notion 集成设计决策：`memory/notion-integration-design.md`
- 设计实现建议用 frontend-design skill（便签是重设计、重手感的功能）
