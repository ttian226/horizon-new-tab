# Cloudflare Pages 正确部署流程

## 问题诊断

你遇到的问题：Deploy command 显示为 "Required（必填）"

**原因：** Cloudflare 将项目识别为 **Workers** 而不是 **Pages**

**解决方法：** 重新创建，选择正确的项目类型

---

## ✅ 正确的部署流程

### 步骤 1: 返回并重新开始

1. 点击当前页面左下角的 **"Back"** 按钮
2. 返回到项目类型选择页面

### 步骤 2: 选择正确的项目类型

在 Cloudflare Dashboard 中：

1. 进入 **Workers & Pages**

2. 点击 **"Create application"**

3. 你会看到两个选项卡：
   - **Workers** ← 不要选这个
   - **Pages** ← 选这个！

4. 确保你在 **"Pages"** 标签下

5. 点击 **"Connect to Git"**

### 步骤 3: 连接 GitHub 仓库

1. 授权 Cloudflare 访问 GitHub（如果还没有）

2. 选择仓库：**ttian226/horizon-new-tab**

3. 点击 **"Begin setup"**

### 步骤 4: 配置构建设置

这是关键步骤！

#### Project name（项目名称）
```
horizon-tab
```

#### Production branch（生产分支）
```
main
```

#### Framework preset（框架预设）⚠️ 重要
在下拉菜单中选择：
- **None** 或
- **Static assets** 或
- 其他"静态"选项

**不要选择：**
- ❌ Cloudflare Workers
- ❌ 任何需要构建的框架

#### Build settings（构建设置）

**Build command（构建命令）:**
```
(留空 - 不填写任何内容)
```

**Build output directory（构建输出目录）:**
```
docs
```

**Root directory（根目录，可选）:**
```
(留空)
```

#### Environment variables（环境变量）
```
(跳过 - 不需要设置)
```

### 步骤 5: 部署

1. 确认所有设置正确
2. 点击蓝色的 **"Save and Deploy"** 按钮
3. 等待部署完成（约 1-2 分钟）

---

## 📊 配置对比

### ❌ 错误配置（Workers 流程）
```
Deploy command: Required (必填)
→ 需要填写 npx wrangler deploy
→ 这是 Workers 的部署方式
```

### ✅ 正确配置（Pages 流程）
```
Framework preset: None 或 Static assets
Build command: (空)
Build output directory: docs
→ 这是 Pages 的部署方式
→ 不需要 Deploy command
```

---

## 🎯 识别正确的界面

### Pages 界面应该有：
- ✅ "Framework preset" 下拉菜单
- ✅ "Build command" (可选)
- ✅ "Build output directory"
- ❌ 没有 "Deploy command" (必填)

### Workers 界面会有：
- ❌ "Deploy command" (必填)
- ❌ "npx wrangler deploy"
- ❌ 这不是我们想要的！

---

## 🚀 部署后的验证

部署成功后，Cloudflare 会显示：

1. **默认域名：**
   ```
   https://horizon-tab-xxx.pages.dev
   ```

2. **测试访问：**
   ```
   https://horizon-tab-xxx.pages.dev/privacy-policy.html
   ```

3. **如果看到隐私政策页面 → 成功！**

---

## 🌐 添加自定义域名

部署成功后：

1. 在项目页面，点击 **"Custom domains"** 标签

2. 点击 **"Set up a custom domain"**

3. 输入域名：
   ```
   horizon-tab.app
   ```

4. Cloudflare 自动配置 DNS（域名在 Cloudflare 托管）

5. 等待几分钟，DNS 生效

6. 访问测试：
   ```
   https://horizon-tab.app/privacy-policy.html
   ```

---

## ⚠️ 常见问题

### Q: 为什么会进入 Workers 流程？

**A:** 可能的原因：
1. 你的仓库中有 `wrangler.toml` 文件
2. 点击了错误的标签（Workers 而不是 Pages）
3. Cloudflare 自动检测错误

### Q: 如何确保选择 Pages？

**A:**
1. 在 "Create application" 页面
2. 确保选择 **"Pages"** 标签（顶部有两个标签）
3. 不要选 "Workers" 标签

### Q: 如果还是看到 Deploy command (Required) 怎么办？

**A:**
1. 停止当前流程
2. 返回首页
3. 确认选择了 **"Pages"** 而不是 "Workers"
4. 重新开始

---

## 📞 需要帮助？

如果重新配置后还有问题：

1. 截图当前页面（显示所有字段）
2. 告诉我你在哪个步骤
3. 我会具体指导

---

## 快速检查清单

在点击 Deploy 前，确认：

- [ ] 选择了 **Pages** 标签（不是 Workers）
- [ ] Framework preset 选择了 "None" 或静态选项
- [ ] Build command 是空的
- [ ] Build output directory 填写了 `docs`
- [ ] **没有看到 "Deploy command (Required)" 字段**

如果有 "Deploy command (Required)"，说明选错了，需要重来！
