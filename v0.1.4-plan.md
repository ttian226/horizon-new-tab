# v0.1.4 — Notion Task 集成

> 开发周期目标：让 Horizon Tab 从"美观的新标签"演化为"Notion 工作流的新标签前哨"。
> 周期开始：2026-05-26
> 主题：**Tasks 接入 Notion**

---

## 战略定位（为什么是这个主题）

新标签页扩展做 todo 的太多了（Momentum / Tabliss / Bonjourr），重做一个无意义。Horizon Tab 的差异化路径：

**「如果你是 Notion 用户，Horizon Tab 就是你的 Notion 在浏览器里的前哨。」**

- 复用用户在 Notion 已有投资，不再要求把数据搬到我们的系统
- 我们只做"展示 + 操作转发"，task 增删改的真源是 Notion
- 维护面骤减（不用做提醒/优先级/视图，Notion 已经做好了）

长期方向（v0.1.5+）：Notion 页面当 Notes 内容源 / Notion DB 当可摆 widget。本期只做 Tasks 一项。

---

## 基建（这分支里已完成）

- [x] **多环境 Firebase 配置** (commit `49b4b2b`)
  - `firebase.ts` 双 config，按 `VITE_FIREBASE_ENV` 切换
  - `npm run build:dev` 本地接 dev project，sed 替换 manifest 里的 OAuth client_id
  - `.firebaserc` 加 production / development 别名
  - 规范文档：Obsidian `学习笔记/前端/Chrome 扩展多环境开发配置.md`
- [x] **Firestore rules 同步到 dev** —— v0.1.4 schema 已 deploy 到 `horizon-dev-734a4`
- [x] **本地 Load Unpacked 验证 Google 登录通过** —— dev OAuth client `425487238446-irlq...` 工作正常，数据落到 dev project
- [x] **Dev seed 工具就位** —— `scripts/seedDev.mjs` + service account JSON 在 `~/Downloads/horizon-dev-734a4-firebase-adminsdk-*.json`，可批量灌测试数据 (commit `3def149`)

---

## 核心任务

### M1 — Notion 读通（先看到）✅ 已完成

让本地能拉 Notion Tasks 到扩展里显示，本地不可改。

- [x] **Settings 加 "Notion" 配置入口** (commit `ffafd02`)
  - SettingsModal 新增 Notion tab：token（密码框）+ DB ID（默认填好）+ Test + Save + Disconnect
  - 存到 `chrome.storage.local`（**注**：必须在 manifest.json 加 `"storage"` 权限，这一步漏过一次）
- [x] **Notion API 客户端层** (commit `ffafd02`) — `src/services/notion.ts`
  - `fetchTasks` + `testConnection`，用 legacy database query endpoint `/v1/databases/{id}/query` + API 版本 `2022-06-28`
  - 字段提取 robust（缺字段不崩，title 拼接 rich text，icon 只取 emoji 类型）
  - **CORS 实战通过**：Chrome extension + `host_permissions: api.notion.com` 绕过 CORS，不需要 Cloud Function 代理
- [x] **数据模型扩展** (commit `5870d2a` → 后续 `3b69613` 简化)
  - 最终决定：**2 态而非 3 态**（matches Notion DB / SyncTask 实际）
  - `CloudTodoItem` 加 `dueDate / icon / notionPageId / lastSyncedAt`，`completed: bool` 仍是 source of truth（不引 `status` 字段）
- [x] **TodoApp / TodoWidget 接入 Notion 模式** (commit `f3b5e1a`)
  - 新 hook `useTodosWithNotion`：Notion 配置存在 → fetch + 5min poll + window focus refresh；否则 → Firestore subscribe
  - 顶部 badge："Synced from Notion · X ago"
  - Notion 模式：行点击 → 新 tab 开 Notion 页；无 add/delete UI；底部 hint "Add and edit tasks in Notion"
- [x] **本地 cache 策略** — 折中：M1 暂不持久化 cache（接入 hook 是直 fetch，无 Firestore 镜像）
  - 断网时 Notion 模式会显示 stale 数据（不报错）+ "Notion: <error>" 红字
  - v0.1.5 / M2 可加正式 cache 层

### M2 — Notion 写通（双向）

本地操作能 push 回 Notion，冲突按 `last_edited_time` 决定胜者。

- [ ] **乐观更新 + 推送**
  - 勾选 / 改 text → 立刻更新本地 UI 和 Firestore cache → debounced 500ms 推 Notion → 成功后写 `lastSyncedAt`
  - 推送失败：toast 提示 + 标记"未同步"小图标，下次开新标签重试
- [ ] **新建 task**
  - 输入框 enter → 先在 Notion 创建（拿 page id）→ 再写本地 cache
  - icon 用 cheat sheet 自动猜（参考 notion-tasks skill 的 emoji 规则）
- [ ] **冲突解决**
  - 开新标签拉 Notion 时，对每条 task：本地 `lastSyncedAt` vs Notion `last_edited_time`，谁新谁胜
  - 简单 last-write-wins，不做 merge —— 个人使用够了
- [ ] **删除**
  - 本地删 → Notion archive（不真删，`archived: true`）
  - Notion archive 的 task → 下次拉取时本地从 cache 中移除

---

## 关键设计决策（已定）

| 决策 | 选择 | 理由 |
|---|---|---|
| **Lists 处理** | 抛弃 lists 概念 | Notion DB 无 list 抽象；现有 lists 是被埋的低使用率功能 |
| **两种模式共存** | 未连 Notion → 本地 todos；连了 Notion → Notion 接管 | 不强迫所有用户用 Notion；保留扩展对纯本地用户的可用性 |
| **历史本地 todos** | 启用 Notion 时**保留**在本地，但不显示 / 不同步 | 防止删数据焦虑；用户停用 Notion 时可恢复 |
| **Token 存放** | `chrome.storage.local` | 敏感，不进 Firestore |
| **Notion 字段映射** | 严格按 SyncTasks 现有 schema（`Name` / `Status` / `Due date` / icon）| 不污染你现有 iOS Widget 工作流 |
| **同步触发** | 开新标签 lazy fetch + 本地操作 debounced push | 不轮询，不开心跳，省 quota |

---

## 附加任务（不属 Notion 主线，但顺手做）

### 壁纸切换无缝化（v0.1.3 体验补丁）

v0.1.3 的 1h 轮换上线后发现的实际问题：轮换时整个屏幕黑 2–3 秒等图下载。手动点"下一张"按钮也同样。

- [x] `wallpaper.ts`: `getRandomWallpaper` 加 `excludeUnsplashId` 参数 (commit `6d027cf`)
- [x] `App.tsx`: preload + crossfade (commit `6d027cf`)
- [x] 验证通过：1h 轮换 / 手动 next 都无黑屏。首次打开的初始黑屏推 v0.1.5 (加占位图)

代码量 ~80 行，2-3 小时。完全独立于 Notion 主线，建议**优先做完这条**再开 Notion（暖手 + 立竿见影 + 给自己信心）。

---

## 不做（推到 v0.1.5+）

| 推迟项 | 推迟原因 |
|---|---|
| **Notion OAuth 一键授权** | 需要 Cloud Function 做 code-exchange（client_secret 不能放扩展端），本期 paste token 够用 |
| **Notion 页面 / DB 作为其它 widget 源** | 等 Tasks 集成稳了再扩 |
| **多 Notion DB 支持** | 单 DB 先跑通 |
| **OAuth token 静默续期**（v0.1.3 留下的）| 优先级让位给 Notion 主线，下版再做 |
| **Streak / 连续打开** | 同上 |
| **Todos 上首页主视觉** | Notion 接入完成后再优化布局 |
| **首次打开壁纸占位图** | 附加任务里只解决了"轮换/切换"无缝；初次打开 cache 全空时仍有黑屏，要加打包的低分辨率占位图，下版做 |

---

## 风险 & 开放问题

1. **Notion API rate limit (3 req/sec)**：开新标签连续拉 + 多次 push 可能撞墙。需要 debounce + 请求队列。
2. **chrome.storage.local 的 token 安全性**：本地存敏感数据有风险（恶意扩展可能扫）。但短期内自用，可接受。多用户 OAuth 版本上线后会用 server-side flow 取代。
3. **冲突的 last-write-wins 漏数据**：极端情况（同一秒两端都改）会丢一端的改动。个人用够了，但写文档时要说明。
4. **现有 prod 用户升级体验**：v0.1.3 用户升级后会看到 What's New 弹窗讲 Notion，但未启用 Notion 时所有行为不变（本地 todos 继续用）。要确保 What's New 文案别让人觉得"我必须接 Notion 才能用"。
5. **dev Firebase 的壁纸数据空缺**：v0.1.3 留下的问题，本期顺手解决 —— 让 wallpapers fetch 始终走 prod Firestore，因为是公开只读数据。

---

## 长远（v0.1.5 起点）

本期不做、但 v0.1.5 要立刻接的：

1. **Notion OAuth Cloud Function**：在 `functions/` 加一个 HTTP function 做 code → token 交换。客户端 Settings 改成"Connect Notion"一键授权
2. **Notion DB 选择 UI**：用户能选自己 workspace 里任意一个 task DB
3. **Notion 字段映射可配置**：用户自己 DB 字段叫"任务"而非 Name 也能用

---

## 发版检查（v0.1.4 提交 Chrome Web Store 前）

- [ ] 本地 dev 测试：登录通过 / Notion token 接入 / M1 read 通 / M2 write 通 / 冲突场景手测
- [ ] 拉一次 prod build (`npm run build`)，确认 manifest client_id 是 prod 的 + Firebase config 是 prod 的（防御）
- [ ] 版本号 0.1.3 → 0.1.4（`package.json` + `public/manifest.json`）
- [ ] `releaseNotes.ts` 加 v0.1.4 条目：**主线** Notion 集成（专属 + 不强制）；**次线** Smoother wallpaper transitions（壁纸切换不再黑屏）
- [ ] `docs/index.html` 加 v0.1.4 卡片，文案突出 Notion 集成
- [ ] `npm run package` → 上 Chrome Web Store + 创建 GitHub Tag/Release
- [ ] merge `feature/v0.1.4` → `main`，本计划文档删除（git 历史保留）

---

## 关联

- 多环境配置规范：Obsidian `学习笔记/前端/Chrome 扩展多环境开发配置.md`
- Notion Tasks DB schema 参考：`~/.claude/skills/notion-tasks/SKILL.md`（同一个 DB，字段定义严格按它来）
- Memory：`memory/local-oauth-broken-by-design.md`
